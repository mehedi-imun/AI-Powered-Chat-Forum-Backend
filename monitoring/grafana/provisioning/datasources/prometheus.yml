apiVersion: 1

datasources:

- name: Prometheus
  type: prometheus
  access: proxy
  url: http://3.1.47.175:9090
  isDefault: true
  editable: true
  jsonData:
    timeInterval: 15s
    queryTimeout: 60s
    httpMethod: POST
  version: 1
